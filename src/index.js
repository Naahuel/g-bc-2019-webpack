/**
 * Los estilos principales los importo desde JS
 * webpack se encarga de compilarlo y linkearlo en el HTML
 */
import "./styles/styles.scss";

/**
 * Acá escribo toda mi lógica JS
 * Usen ES6, importen otros módulos, hagan lo que quieran
 * webpack se encarga de compilarlo y pasarlo por Babel para que
 * se genere código más compatible
 * Vamos a ir importándo por módulos para separar la lógica de mi app
 */
import "./js/module.js";
